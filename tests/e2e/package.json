{
	"name": "homburg-dealerportaal-e2e",
	"private": true,
	"description": "Browser-level (Playwright) smoke tests voor het dealerportaal — vult de bestaande PHPUnit-testset (../..) aan, die geen CSS/lay-out/klikgedrag kan controleren.",
	"scripts": {
		"test": "playwright test",
		"test:headed": "playwright test --headed",
		"report": "playwright show-report"
	},
	"devDependencies": {
		"@playwright/test": "^1.48.0",
		"dotenv": "^16.4.5"
	}
}
